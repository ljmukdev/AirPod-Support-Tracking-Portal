# Add this to your .env file to enable AI-powered feedback generation

# Anthropic API Key (get from: https://console.anthropic.com/)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxx

# The system will automatically use AI if the key is configured
# If not configured, it will fall back to template-based feedback
